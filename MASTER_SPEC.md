This is your MASTER_SPEC.md. This document is the source of truth for your project. When you want to start coding, you feed this to me, and we execute phase-by-phase. Do not deviate from these specifications without updating the document first.MASTER_SPEC.md: IELTS Platform Architecture (Production Grade)1. System OverviewA closed-access, hierarchical platform for managing IELTS course centers, teachers, students, and testing. There is no public sign-up. All accounts are provisioned via the established hierarchy.2. Technology StackProgramming Language: Python 3.11+Framework: FastAPI (Asynchronous, high-performance, ideal for AI integration).Database: MongoDB (NoSQL) with Motor or Beanie (ODM) for async operations.Infrastructure: Docker + Docker Compose.Web Server / Proxy: Nginx (Reverse proxy, SSL/TLS termination).File Storage: Cloudflare R2 (S3-compatible API).Deployment: CI/CD via GitHub Actions.Monitoring: Uptime Kuma.3. Hierarchical Access Control (RBAC)All actions are scoped by the user's role and their assigned center_id or group_id.RolePermissionsCreation AuthoritySuperAdminSystem-wide control.System InitializerAdminManages one Course Center.Created by SuperAdminTeacherManages Groups/Students in their center.Created by AdminStudentTakes tests, views own progress.Created by Teacher4. Database Schema (The "Source of Truth")4.1. users Collection_id: ObjectIdusername: string (Unique)password_hash: string (Bcrypt)role: enum ["superadmin", "admin", "teacher", "student"]center_id: ObjectId (Optional, null for SuperAdmin)group_id: ObjectId (Optional, relevant for Students/Teachers)metadata: object (full_name, contact)4.2. centers Collection_id: ObjectIdname: stringadmin_id: ObjectId (Reference to the Admin user)4.3. groups Collection_id: ObjectIdname: stringcenter_id: ObjectIdteacher_id: ObjectId4.4. tests Collection_id: ObjectIdcenter_id: ObjectIdauthor_id: ObjectIdtitle: stringcontent_json: object (Schema for questions, options, correct_answers)created_at: datetime4.5. results Collection_id: ObjectIdtest_id: ObjectIdstudent_id: ObjectIdscore: floatanswers: array (List of user choices)completed_at: datetime5. Security & AuthenticationAuth: JWT (JSON Web Tokens) with short expiration time.SuperAdmin Init: On first startup, the system checks users collection. If empty:Insert username: "Jamal"Insert password: "Sbros0803" (Hashed)Set role: "superadmin"Middleware: Every protected endpoint must use a RoleChecker dependency to verify the JWT and the user's role vs. the requested resource.Env Variables: No secrets in code. Use .env file for:MONGO_URIJWT_SECRET_KEYADMIN_PASSWORD (Initial)6. Functional Workflows6.1. User Creation (Top-Down Flow)No Sign-Up: All users are created by the layer above them.POST /admin/create-teacher -> Requires Admin role.POST /teacher/create-student -> Requires Teacher role.6.2. Test WorkflowAdmin adds a test to the tests collection.Teacher assigns a test_id to a group_id (creating an Assignment).Student queries GET /tests/assigned -> Filtered by their group_id.Student submits answers -> POST /tests/submit -> Calculate score -> Save to results.7. Infrastructure DetailsDockerization: Every component (FastAPI, MongoDB, Nginx) must run in isolated containers.Network: docker-compose internal network. MongoDB port 27017 must not be mapped to host. Access only via app container.Logging: All API errors and access attempts to be logged via Python's standard logging library to stdout/stderr.8. Phase-Based RoadmapPhase 0: Environment Setup: Dockerfile, docker-compose.yml, FastAPI boilerplate, requirements.txt setup.Phase 1: Core Auth & Bootstrapping: Database connection, JWT implementation, SuperAdmin initialization script.Phase 2: User CRUD: API endpoints for SuperAdmin/Admin/Teacher to provision new users.Phase 3: Test Engine: JSON-based test creation and storage schema.Phase 4: Submission Logic: Scoring algorithm and results storage.Phase 5: Analytics & Security: Role-based access enforcement, logging, and performance hardening.Instructions for AI:Strict Compliance: Any code or architectural change must align with this spec.No Hallucination: If a feature isn't in this spec, do not implement it. Ask for clarification first.Language: Code must be written in Python (FastAPI).Environment: All infrastructure commands are for Ubuntu/Debian-based Linux environments via CLI.This is your Master Specification. Save it. When you are ready to begin, tell me to "Start Phase 0," and we will build the foundation.